# Source: original fixture

Authored for the trustabl test corpus. Exercises:

- `.claude/settings.json` permissions parsing
- `.claude/agents/*.md` subagent frontmatter parsing
- Claude `AgentDefinition` typed-accessor coverage:
  `tools`, `disallowedTools`, `permissionMode`, `mcpServers`.

No external code. Public-domain.
